# GLOBAL VARIABLES for edxtras

globalVariables(
    c("index", "data", "output", "metric", "everything", ":=", "state.abb",
        "str_to_lower", "zipcodes", "center", "is", "lcl", "ucl", "violation"
    )
)
